PORT=3001
NODE_ENV=development
STORAGE_DIR=./storage

# OCR Configuration
# Available modes: 'local' (tesseract.js Devanagari/Sanskrit), 'external'
OCR_DEFAULT_PROVIDER=local
OCR_LANGUAGES=san+hin

# Optional External OCR API keys (if configured)
GOOGLE_VISION_API_KEY=
OPENAI_API_KEY=
